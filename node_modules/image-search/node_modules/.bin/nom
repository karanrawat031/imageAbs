#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../nom/bin/nom" "$@"
  ret=$?
else 
  node  "$basedir/../nom/bin/nom" "$@"
  ret=$?
fi
exit $ret
